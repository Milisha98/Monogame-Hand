namespace Hands.GameObjects.Enemies.Turret;
public enum TurretState
{
    Closed,
    Opening,
    Raising,
    Active,
    Destroyed,
    Lowering,
    Closing
}

public enum TurretStyle
{
    Style1,
    Style2
}